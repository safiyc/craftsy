<%= simple_form_for ([@product, @product.reviews.build]) do |f| %>
  <div id="rating-form">
    <%= f.input :rating %>
  </div>
  <%= f.input :comment %>
  <%= f.button :submit, class: "btn-custom2" %>
<% end %>

<!-- this is part of jquery.raty -->
<script>
  $('#rating-form').raty({
    path: '/assets/',
    scoreName: 'review[rating]'
  });
</script>
